export const metadata = {
  title: "BPSense | Petugas",
  description: "Aplikasi untuk Petugas BPS",
};

function PetugasLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export default PetugasLayout;
